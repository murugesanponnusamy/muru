kind: Pod
apiVersion: v1
metadata:
  name: apple-app-db
  labels:
    app: apple
spec:
  containers:
    - name: apple-app-db
      image: hashicorp/http-echo
      args:
        - "-text=apple"

---

kind: Service
apiVersion: v1
metadata:
  name: db-service
spec:
  selector:
    app: apple-app-db
  ports:
    - port: 5678 # Default port for image
